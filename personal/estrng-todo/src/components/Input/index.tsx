import styles from './styles.module.css';

type IInputProps = {
  addText: (text: string) => void;
  text: string;
}

export default function Input({ addText, text }: IInputProps): JSX.Element {
  
  return (
    
  )
}
